import { Text, View } from "react-native";

export default function Wishlist() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#fff",
      }}
    >
      <Text>Wishlist</Text>
    </View>
  );
}
