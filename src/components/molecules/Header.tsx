import LogoIcon from "@atoms/LogoIcon";
import { View } from "react-native";

const Header = () => (
  <View className="flex items-center justify-center w-full h-32 bg-secondary">
    <LogoIcon />
  </View>
);

export default Header;
